{
  "name": "code-in-the-dark",
  "author": "Alexander Chabin",
  "license": "MIT",
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "npm run build --workspace=client && npm run build --workspace=server && npm run start --workspace=server",
    "dev": "NODE_ENV=development concurrently 'npm run dev --workspace=server' 'npm run start --workspace=client'",
    "typecheck": "npm run typecheck --workspace=client && npm run typecheck --workspace=server"
  },
  "devDependencies": {
    "concurrently": "^9.2.0"
  }
}
