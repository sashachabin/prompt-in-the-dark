{
  "name": "code-in-the-dark",
  "license": "MIT",
  "author": "Alexander Chabin",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "start": "node scripts/run.mjs start",
    "dev": "node scripts/run.mjs dev",
    "start:core": "npm run build --workspace=client && npm run build --workspace=server && npm run start --workspace=server",
    "dev:core": "NODE_ENV=development concurrently 'npm run dev --workspace=server' 'npm run start --workspace=client'",
    "typecheck": "npm run typecheck --workspace=client && npm run typecheck --workspace=server",
    "lint": "oxlint client server",
    "lint:fix": "oxlint --fix client server",
    "format": "oxfmt",
    "format:check": "oxfmt --check"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "oxfmt": "^0.45.0",
    "oxlint": "^1.60.0"
  },
  "engines": {
    "node": ">=18"
  }
}
